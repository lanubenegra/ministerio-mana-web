<!-- Premium Church Selector Component -->
<div id="church-selector-modal" class="fixed inset-0 z-[60] hidden items-center justify-center bg-black/60 backdrop-blur-sm p-4">
  <div class="w-full max-w-2xl bg-gradient-to-br from-[#293C74] to-[#1e2b58] rounded-3xl shadow-2xl border border-white/10 overflow-hidden">
    
    <!-- Header -->
    <div class="p-6 border-b border-white/10">
      <div class="flex items-center justify-between mb-4">
        <h3 class="text-xl font-bold text-white">Seleccionar Iglesia</h3>
        <button id="close-church-selector" class="text-white/60 hover:text-white transition-colors">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
      
      <!-- Search Bar -->
      <div class="relative">
        <svg xmlns="http://www.w3.org/2000/svg" class="absolute left-4 top-1/2 -translate-y-1/2 h-5 w-5 text-white/40" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
        </svg>
        <input 
          type="text" 
          id="church-search-input" 
          placeholder="Buscar por nombre, ciudad o país..." 
          class="w-full bg-white/10 border border-white/20 rounded-xl pl-12 pr-4 py-3 text-white placeholder-white/40 focus:border-brand-teal focus:ring-2 focus:ring-brand-teal/50 outline-none transition-all"
        >
      </div>
    </div>

    <!-- Filters -->
    <div class="px-6 py-4 bg-black/20 border-b border-white/10">
      <div class="grid grid-cols-2 gap-3">
        <select id="church-country-filter" class="bg-white/10 border border-white/20 rounded-xl px-4 py-2 text-white text-sm focus:border-brand-teal focus:ring-2 focus:ring-brand-teal/50 outline-none transition-all">
          <option value="">Todos los países</option>
          <!-- Populated dynamically -->
        </select>
        <select id="church-city-filter" class="bg-white/10 border border-white/20 rounded-xl px-4 py-2 text-white text-sm focus:border-brand-teal focus:ring-2 focus:ring-brand-teal/50 outline-none transition-all">
          <option value="">Todas las ciudades</option>
          <!-- Populated dynamically -->
        </select>
      </div>
    </div>

    <!-- Special Options -->
    <div class="px-6 py-3 bg-black/10 border-b border-white/10">
      <div class="flex flex-wrap gap-2">
        <button data-special="virtual" class="special-church-btn px-4 py-2 bg-white/5 hover:bg-brand-teal/20 border border-white/10 hover:border-brand-teal rounded-xl text-xs font-bold text-white/80 hover:text-white transition-all">
          🌐 Ministerio Maná Virtual
        </button>
        <button data-special="none" class="special-church-btn px-4 py-2 bg-white/5 hover:bg-brand-teal/20 border border-white/10 hover:border-brand-teal rounded-xl text-xs font-bold text-white/80 hover:text-white transition-all">
          ⛪ No asisto a ninguna iglesia
        </button>
        <button data-special="other" class="special-church-btn px-4 py-2 bg-white/5 hover:bg-brand-teal/20 border border-white/10 hover:border-brand-teal rounded-xl text-xs font-bold text-white/80 hover:text-white transition-all">
          ➕ Otra iglesia
        </button>
      </div>
    </div>

    <!-- Results List (Virtual Scroll) -->
    <div class="h-96 overflow-y-auto" id="church-results-container">
      <div id="church-results-list" class="divide-y divide-white/5">
        <!-- Populated dynamically -->
      </div>
      <div id="church-no-results" class="hidden text-center py-12 text-white/40">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 mx-auto mb-3 opacity-30" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <p class="text-sm">No se encontraron iglesias</p>
      </div>
      <div id="church-loading" class="hidden text-center py-12">
        <div class="inline-block h-8 w-8 animate-spin rounded-full border-4 border-solid border-brand-teal border-r-transparent"></div>
        <p class="text-white/60 text-sm mt-3">Cargando iglesias...</p>
      </div>
    </div>

    <!-- Footer -->
    <div class="p-4 bg-black/20 border-t border-white/10 text-center">
      <p class="text-xs text-white/40">
        Selecciona una iglesia para continuar con el registro
      </p>
    </div>

  </div>
</div>

<style>
  .church-item {
    @apply px-6 py-4 hover:bg-white/5 cursor-pointer transition-colors;
  }
  
  .church-item:hover {
    @apply bg-gradient-to-r from-brand-teal/10 to-transparent;
  }
  
  .church-item.selected {
    @apply bg-brand-teal/20 border-l-4 border-brand-teal;
  }
  
  .special-church-btn:hover {
    @apply scale-105 shadow-lg shadow-brand-teal/20;
  }
</style>
